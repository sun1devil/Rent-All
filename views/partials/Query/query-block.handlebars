
<div class="itemsRent">
    <img src="/assets/Images/dog.jpg" width="75px">
        <div>
            <h5>{{description}}</h5>
            <p>Owner: {{item_name}}</p>
            <p>Availability: {{start_date}} to {{end_date}} </p>
            <button class="change-rent"  data-id="{{id}}" data-newtodo="{{available}}">{{#if available}}RENT for {{price}}{{else}}Unavailable{{/if}} 
        </div>
</div>
