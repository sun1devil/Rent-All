    {{!-- <div class="card avail" style="width: 18rem;">
        <img class="card-img-top" src="/assets/img/dog.jpg" alt={{item_name}}>
        <div class="card-body">
            <h5 class="card-title">{{item_name}}</h5>
            <p class="card-text">{{start_date}}</p>
            <p class="card-text">{{end_date}}</p>
            <p class="card-text">{{owner_id}}</p>
            <button data-id={{id}} class="rent-btn">RENT for {{price}}</button>
        </div>
    </div> --}}

<div style="width: 23rem;background:lightblue; opacity:0.7; display:flex;justify-content:center;align-items:center;">
    <img src="/assets/Images/dog.jpg" width="75px">
        <div>
            <h5>{{description}}</h5>
            <p>Owner: {{item_name}}</p>
            <p>Availability: {{start_date}} to {{end_date}} </p>
            <button class="change-rent"  data-id="{{id}}" data-newtodo="{{rent}}">{{#if rent}}RENT for {{price}}{{else}}Unavailable{{/if}} 
        </div>
</div>
