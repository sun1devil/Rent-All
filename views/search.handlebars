<h1> Rent All'Em Items</h1>

<h2>Available Items to Rent Out Bruh:</h2>

{{#each items}}
{{#if available}}
    {{> Query/query-block}}
{{/if}}
{{/each}}

<h2>U SOL man, these are unavailable:</h2>

{{#each items}}
{{#unless available}}
    {{> Query/query-block}}
{{/unless}}
{{/each}}

{{!-- $(".rent-btn").on("click", function(){ --}}
    {{!-- var itemID = $(this).attr("data-id"); --}}

{{!-- }); --}}

{{!-- 
<header>
  <h2>Search for items:</h2>
</header>

<form class="create-form">

  <div class="form-group">
    <label for="forrent"> Item Name:</label>
    <input type="text" id="forrent" name="name">
  </div>

  <button type="submit">Search for items</button>
</form> --}}